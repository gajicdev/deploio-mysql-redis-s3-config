apiVersion: v1
kind: Service
metadata:
  name: {{ include "deploio.fullname" . }}
  labels:
    {{- include "deploio.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "deploio.selectorLabels" . | nindent 4 }}
