apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "deploio.fullname" . }}-test-connection"
  labels:
    {{- include "deploio.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "deploio.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
